import Button from "@mui/material/Button"
import React from "react"

export function SubmitButton() {
    return <Button variant="contained" type="submit" style={{marginTop: "1em"}}>
        Submit
    </Button>
}